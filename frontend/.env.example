## Frontend (Vite) - variáveis de ambiente

# Origem/base da API. Pode ser:
# - absoluto: `http://127.0.0.1:8010`
# - relativo atrás de proxy/nginx: `/api/v1`
# Se não definido, usa a origem atual do browser e o prefixo abaixo.
# Exemplo recomendável em produção atrás do Nginx: `/api/v1`
VITE_API_BASE_URL=

# Prefixo de versão do backend (path).
# Padrão: `/api/v1`
# Se `VITE_API_BASE_URL` for absoluto, este prefixo é anexado aos endpoints.
VITE_API_VERSION_PREFIX=/api/v1

# Intervalo do polling em segundos para atualização do dashboard em tempo real.
# Padrão: 15 segundos
VITE_REALTIME_POLL_INTERVAL_SEC=15

# Intervalo do carrossel de categorias.
# Defina UM dos dois. Se ambos forem definidos, `MS` tem precedência.
# Padrão: 15000 ms (15s) se nenhum for fornecido.
VITE_CATEGORY_CAROUSEL_INTERVAL_MS=
VITE_CATEGORY_CAROUSEL_INTERVAL_SEC=